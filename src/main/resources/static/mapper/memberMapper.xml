<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.eutchapedia.login.mapper.MemberMapper">

	<!-- 회원 가입 -->
    <insert id="signup">
	     insert into member(member_email, member_nickname, member_pwd)
	     values(#{memberEmail}, #{memberNickname}, #{memberPwd})   
    </insert>
	
	<!-- 로그인 -->
	<select id="login" resultType="kr.or.eutchapedia.login.vo.MemberVo">
		select memberEamil, memberPwd from member
		where memberEamil = #{memberEmail} and memberPwd = #{memberPwd}
	</select>
</mapper>