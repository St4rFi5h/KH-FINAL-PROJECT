<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.eutchapedia.login.dao.MemberDaoInterface">

	<!-- 회원 가입 -->
    <insert id="signup" parameterType="kr.or.eutchapedia.login.vo.MemberVo">
	     insert into member
	     values
	     (#{memberEmail}, 
	      #{memberNickname}, 
	      #{memberPwd}, 
	      #{memberPwdSalt}, default, default, default, null, null, default)   
    </insert>
    
    <!-- 이메일 중복 체크 -->
    <select id="emailchk" parameterType="kr.or.eutchapedia.login.vo.MemberVo">
    	select count(*) from member where member_email = #{memberEmail}
    </select>
    
    <!-- 닉네임 중복 체크 -->
    <select id="nicknamechk" parameterType="kr.or.eutchapedia.login.vo.MemberVo">
    	select count(*) from member where member_nickname = #{memberNickname}
    </select>
	
	<!-- 로그인 -->
	<select id="login" resultType="kr.or.eutchapedia.login.vo.MemberVo">
		select member_email, member_pwd from member
		where member_email = #{memberEmail} and member_pwd = #{memberPwd}
	</select>
</mapper>